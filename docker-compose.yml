services:
  redis:
    image: "redis:7-alpine"
    ports:
      - "6379:6379"
    sysctls:
      - net.core.somaxconn=1024
      - vm.overcommit_memory=1
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    volumes:
      - ./frontend:/app
    ports:
      - "30000:80"
